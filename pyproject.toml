[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
filterwarnings = [
    "ignore:distutils Version classes are deprecated. Use packaging.version instead.*:DeprecationWarning::",
    "ignore:Using 'method_whitelist' with Retry is deprecated and will be removed in v2.0*:DeprecationWarning:arcgis.*:",
    "ignore:An exception was ignored while fetching the attribute `__array_interface__` from an object of type 'Polygon'*:DeprecationWarning:geopandas.*:"
]